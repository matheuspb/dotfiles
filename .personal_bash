#!/bin/sh
#
#   Put this file on home folder or link with 'ln',
# and put the lines below in ~/.bashrc
#
# if [ -f ~/.personal_bash ]; then
#     . ~/.personal_bash
# fi

alias ll='ls -l'
alias la='ls -A'
alias lla='ls -lA'

alias mv='mv -i' # prompt before overwrite
alias cp='cp -i' # prompt before overwrite
alias xflux='xflux -l -27.6 -g -48.5'

alias bye='sudo shutdown 0'
alias usage='du -bsh'
alias mount-data='sudo mount -t auto -v /dev/sda6 /media/matheus/Data/'

function cs () { cd "$@" && ls; }

PS1='\W$(__git_ps1) $ ' # prompt

